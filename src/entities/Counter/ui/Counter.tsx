import React from 'react';

const Counter = () => {
    return (
        <div>
<h1>
    counterValue
</h1>
            <button>increment</button>
            <button>decrement</button>
        </div>
    );
};

export default Counter;
